const initialState = {
  collection: [],
  isLoading: false,
};
const AllUsersReducers = (state = initialState, action) => {
  switch (action.type) {
    case "value":
      break;

    default:
      return state;
      break;
  }
};

export default AllUsersReducers;
