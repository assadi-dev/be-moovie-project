const initialState = {
  collections: [],
  isLoading: false,
};

const AllPostReducers = (state = initialState, action) => {
  switch (action.type) {
    case "value":
      break;

    default:
      return state;
      break;
  }
};

export default AllPostReducers;
